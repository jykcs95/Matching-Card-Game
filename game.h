#ifndef GAME_H
#define GAME_H

void swap(char *a, char *b);
void printArray(char arr[], int n, char* output);
void randomize(char arr[], int n);
int check(char x);
void game(char* output, char*);

#endif
